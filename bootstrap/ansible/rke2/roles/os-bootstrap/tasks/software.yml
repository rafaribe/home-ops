---
- name: upgrade the OS and all packages
  apt:
    update_cache: yes

- name: install basic software environment
  apt:
    name: "{{ packages }}"
    state: present
  vars:
    packages:
      #- wireguard
      - neovim
      - lvm2
      - wipe
      - neofetch