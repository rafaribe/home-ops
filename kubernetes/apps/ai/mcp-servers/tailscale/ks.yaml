---
# yaml-language-server: $schema=https://ks.hsn.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: tailscale-mcp
  namespace: flux-system
spec:
  commonMetadata:
    labels:
      app.kubernetes.io/name: tailscale-mcp
  dependsOn:
    - name: external-secrets-stores
      namespace: external-secrets
    - name: toolhive-operator-crds
  interval: 1h
  path: ./kubernetes/apps/ai/mcp-servers/tailscale
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: ai
  timeout: 5m
