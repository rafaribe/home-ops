---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app portabase
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
  interval: 1h
  values:
    controllers:
      portabase:
        annotations:
          glance/description: Database Backup Tool
          glance/icon: mdi:database-export
          glance/id: portabase
          glance/name: Portabase
          glance/url: https://portabase.rafaribe.com
          reloader.stakater.com/auto: "true"
        strategy: Recreate
        containers:
          app:
            image:
              repository: docker.io/portabase/portabase
              tag: 1.30.2@sha256:4d793c9041e3170b73045448e3d58a8ee76cba2fc35ae5793ebcfc921efb4b5f
            env:
              NODE_ENV: production
              PROJECT_URL: "https://portabase.rafaribe.com"
              TZ: Europe/Lisbon
            envFrom:
              - secretRef:
                  name: portabase
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
            resources:
              requests:
                cpu: 25m
                memory: 256Mi
              limits:
                memory: 1Gi
            securityContext:
              runAsUser: 0
    defaultPodOptions:
      securityContext:
        runAsNonRoot: false
        seccompProfile:
          type: RuntimeDefault
    persistence:
      data:
        existingClaim: *app
        globalMounts:
          - path: /data
    route:
      app:
        annotations:
          gethomepage.dev/description: Database Backup Tool
          gethomepage.dev/enabled: "true"
          gethomepage.dev/group: Database
          gethomepage.dev/icon: https://raw.githubusercontent.com/Portabase/portabase/main/public/portabase.svg
          gethomepage.dev/name: Portabase
          gethomepage.dev/siteMonitor: http://portabase.database:80
        hostnames: ["portabase.rafaribe.com"]
        parentRefs:
          - name: envoy-internal
            namespace: network
            sectionName: https
        rules:
          - backendRefs:
              - name: *app
                port: 80
    service:
      app:
        controller: portabase
        ports:
          http:
            port: 80
