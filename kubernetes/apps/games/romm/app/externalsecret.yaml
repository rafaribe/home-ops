---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1beta1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: &name romm
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: akeyless-secret-store
  target:
    name: *name
    template:
      engineVersion: v2
      data:
        # App
        DB_HOST: &dbHost home-ops-rw.database.svc.cluster.local
        DB_NAME: &dbName romm
        DB_PASSWD: &dbPass "{{ .POSTGRES_GENERIC_APP_PASSWORD }}"
        DB_PORT: "5432"
        DB_USER: &dbUser "romm"
        DISABLE_USERPASS_LOGIN: "false"
        IGDB_CLIENT_ID: "{{ .IGDB_CLIENT_ID }}"
        IGDB_CLIENT_SECRET: "{{ .IGDB_CLIENT_SECRET }}"
        REDIS_HOST: romm-dragonfly.games.svc.cluster.local
        REDIS_PORT: "6379"
        RETROACHIEVEMENTS_API_KEY: "{{ .RETROACHIEVEMENTS_API_KEY }}"
        ROMM_AUTH_SECRET_KEY: "{{ .ROMM_AUTH_SECRET_KEY }}"
        STEAMGRIDDB_API_KEY: "{{ .STEAMGRIDDB_API_KEY }}"
        # OIDC
        OIDC_ENABLED: "true"
        OIDC_PROVIDER: custom
        OIDC_CLIENT_ID: "{{ .OIDC_CLIENT_ID }}"
        OIDC_CLIENT_SECRET: "{{ .OIDC_CLIENT_SECRET }}"
        OIDC_REDIRECT_URI: https://romm.rafaribe.com/api/oauth/openid
        OIDC_SERVER_APPLICATION_URL: https://idm.rafaribe.com/oauth2/openid/romm
        # Postgres Init
        INIT_POSTGRES_DBNAME: *dbName
        INIT_POSTGRES_HOST: *dbHost
        INIT_POSTGRES_PASS: *dbPass
        INIT_POSTGRES_SUPER_PASS: "{{ .POSTGRES_SUPER_PASS }}"
        INIT_POSTGRES_USER: *dbUser
  dataFrom:
    - extract:
        key: /cloudnativepg
    - extract:
        key: /romm
