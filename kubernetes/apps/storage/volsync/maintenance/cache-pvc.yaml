apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kopia-maintenance-cache
  namespace: storage
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 32Gi
