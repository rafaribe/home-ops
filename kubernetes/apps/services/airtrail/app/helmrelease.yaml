---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app airtrail
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
  interval: 30m
  values:
    controllers:
      airtrail:
        annotations:
          glance/description: Flight Tracker
          glance/icon: mdi:airplane
          glance/id: airtrail
          glance/name: AirTrail
          glance/url: https://airtrail.rafaribe.com
          reloader.stakater.com/auto: "true"
        containers:
          app:
            env:
              ORIGIN: https://airtrail.rafaribe.com
              UPLOAD_LOCATION: /data/uploads
            envFrom:
              - secretRef:
                  name: airtrail
            image:
              repository: ghcr.io/johanohly/airtrail
              tag: v3.10.0
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
            resources:
              limits:
                memory: 512Mi
              requests:
                cpu: 10m
                memory: 128Mi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop:
                  - ALL
              readOnlyRootFilesystem: true
        initContainers:
          init-db:
            envFrom:
              - secretRef:
                  name: airtrail
            image:
              repository: ghcr.io/home-operations/postgres-init
              tag: 18.6@sha256:b6d3af974df781c673d37e49bdddfa14a6f5be28b18d2cb7713f0449bea4057b
    defaultPodOptions:
      securityContext:
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        runAsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile:
          type: RuntimeDefault
    persistence:
      data:
        existingClaim: airtrail
        globalMounts:
          - path: /data/uploads
      tmp:
        globalMounts:
          - path: /tmp
        type: emptyDir
    route:
      app:
        hostnames:
          - airtrail.rafaribe.com
        parentRefs:
          - name: envoy-internal
            namespace: network
            sectionName: https
        rules:
          - backendRefs:
              - name: airtrail
                port: 3000
    service:
      app:
        controller: airtrail
        ports:
          http:
            port: 3000
