---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hajimari-config
  namespace: services
spec:
  storageClassName: config-nfs
  volumeName: hajimari
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 128Mi
