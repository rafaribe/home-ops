---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cluster-apps-nfs-subdir
  namespace: flux-system
spec:
  interval: 10m
  path: "./apps/kube-system/nfs-subdir-external-provisioner/app"
  prune: true
  sourceRef:
    kind: OCIRepository
    name: home-ops
  wait: true
  postBuild:
    substituteFrom: []
