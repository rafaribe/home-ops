---
apiVersion: "cilium.io/v2alpha1"
kind: CiliumLoadBalancerIPPool
metadata:
  name: "primary-pool"
spec:
  cidrs:
    - cidr: ${NETWORK_BGP_LB_CIDR}
  serviceSelector:
    matchLabels:
      pool: lb
