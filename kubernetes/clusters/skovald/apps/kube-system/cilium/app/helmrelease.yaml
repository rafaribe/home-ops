---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: cilium
  namespace: kube-system
  annotations:
    meta.helm.sh/release-name: cilium
    meta.helm.sh/release-namespace: kube-system
  labels:
    app.kubernetes.io/managed-by: Helm
spec:
  interval: 5m
  chart:
    spec:
      chart: cilium
      version: 1.13.1
      sourceRef:
        kind: HelmRepository
        name: cilium-charts
        namespace: flux-system
      interval: 5m
  install:
    createNamespace: true
    remediation: # perform remediation when helm install fails
      retries: 3
  upgrade:
    remediation: # perform remediation when helm upgrade fails
      retries: 3
      remediateLastFailure: true # remediate the last failure, when no retries remain
    cleanupOnFail: true
  values:
    autoDirectNodeRoutes: true
    localRedirectPolicy: true
    externalIPs:
      enabled: false
    bgpControlPlane:
      enabled: true
    bgp:
      announce:
        loadbalancerIP: true
      enabled: false
    cluster:
      id: ${CLUSTER_ID}
      name: ${CLUSTER_NAME}
    containerRuntime:
      integration: containerd
    endpointRoutes:
      enabled: true
    hubble:
      enabled: true
      metrics:
        enabled:
          - dns:query;ignoreAAAA
          - drop
          - tcp
          - flow
          - port-distribution
          - icmp
          - http
      relay:
        enabled: true
        rollOutPods: true
      serviceMonitor:
        enabled: true
      ui:
        enabled: true
        ingress:
          annotations:
            external-dns.alpha.kubernetes.io/hostname: &host hubble.${EXTERNAL_DOMAIN}
            cert-manager.io/cluster-issuer: letsencrypt-production
          enabled: true
          hosts:
            - *host
          tls:
            - hosts:
                - *host
        rollOutPods: true
    ipam:
      mode: kubernetes
    ipv4NativeRoutingCIDR: ${K8S_POD_CIDR}
    k8sServiceHost: skovald.${INTERNAL_DOMAIN}
    k8sServicePort: 6443
    kubeProxyReplacement: strict
    kubeProxyReplacementHealthzBindAddr: 0.0.0.0:10356
    loadBalancer:
      algorithm: maglev
      mode: dsr
    operator:
      rollOutPods: true
      replicas: 1
      prometheus:
        enabled: true
        serviceMonitor:
          enabled: true
    rollOutCiliumPods: true
    securityContext:
      privileged: true
    tunnel: disabled
