apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: postgres
  namespace: database
spec:
  schedule: "*/2 * * * *"
  immediate: true
  backupOwnerReference: self
  cluster:
    name: postgres
