---
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: default-pool
  namespace: network-system
spec:
  addresses:
    - ${K8S_LB_L2_CIDR}
  autoAssign: true
---
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: home-network-pool
  namespace: network-system
spec:
  addresses:
    - ${HOME_NETWORK_L2_CIDR}
  autoAssign: true
