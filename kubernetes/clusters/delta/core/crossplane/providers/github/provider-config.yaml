apiVersion: github.jet.crossplane.io/v1alpha1
kind: ProviderConfig
metadata:
  name: jet-github-config
spec:
  credentials:
    source: Secret
    secretRef:
      name: github-creds
      namespace: crossplane-system
      key: token