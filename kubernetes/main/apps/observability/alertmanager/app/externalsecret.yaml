apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: &name alertmanager-secret
  namespace: observability
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: akeyless-secret-store
  target:
    name: *name
    creationPolicy: Owner
    deletionPolicy: "Delete"
    template:
      engineVersion: v2
      data:
        pushover_api_token: "{{ .PUSHOVER_USER_KEY }}"
        pushover_api_userkey: "{{ .ALERTMANAGER_PUSHOVER_TOKEN }}"
  dataFrom:
    - extract:
        key: /alertmanager
