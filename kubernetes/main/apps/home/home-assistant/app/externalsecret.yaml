---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: &name hass
  namespace: home
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: akeyless-secret-store
  target:
    name: home-assistant
  dataFrom:
    key: /hass
