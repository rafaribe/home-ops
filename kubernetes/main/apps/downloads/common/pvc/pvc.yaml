---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ceph-media
spec:
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 1024Gi
  storageClassName: ceph-filesystem
